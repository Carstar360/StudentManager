#include "student.h"
using namespace std;
#include<string>
void student::setName(string first, string last)
{
    first = "";
    last = "";
}

string student::fullName()
{
    string first;
    string last;
    string value;
    value = first.append(last);
    return value;
}

